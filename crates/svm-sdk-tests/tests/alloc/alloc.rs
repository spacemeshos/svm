use svm_sdk::app;

#[app]
mod App {
    // The `svm_alloc` is auto-generated.
    //
    // #[no_mangle]
    // pub extern "C" fn svm_alloc(size: u32) -> u32 {
    //   // allocate...
    //}
}

fn main() {
    // let size = 10u32;
    // let offset: u32 = svm_alloc(size);

    // assert!(offset > 0);
}
