use crate::{helpers, use_gas};

use wasmer_runtime::Ctx as WasmerCtx;

use byteorder::{BigEndian, ByteOrder};

pub fn get64(ctx: &mut WasmerCtx, var_id: u32) -> u64 {
    todo!()
}

pub fn set64(ctx: &mut WasmerCtx, var_id: u32) {
    //
}
