[package]
name = "svm-genesis-tests"
version = "0.0.0"
authors = ["Spacemesh SVM Team"]
license = "MIT"
edition = "2021"
readme = "README.md"
repository = "https://github.com/spacemeshos/svm"
homepage = "https://github.com/spacemeshos/svm"
description = "Spacemesh Virtual Machine"
publish = false

[dependencies]
svm-codec = { path = "../../crates/codec" }
svm-genesis-config = { path = "../../crates/genesis-config" }
svm-runtime = { path = "../../crates/runtime" }
svm-state = { path = "../../crates/state" }
svm-types = { path = "../../crates/types" }
tokio = { version = "1", features = ["full"] }

[features]
